version: 2

models:
  - name: stg_address
    description: Staging model for addresses
    columns:

      - name: address_id
        description: The identifier of the address
        tests:
          - unique
          - not_null
